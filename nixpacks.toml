[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "ls -la",
  "cat pnpm-lock.yaml | head -5",
  "npm install -g pnpm@10.27.0",
  "pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = ["pnpm run build:web"]

[start]
cmd = "pnpm run start:web"
