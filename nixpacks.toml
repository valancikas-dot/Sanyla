[phases.setup]
nixPkgs = ["nodejs_20"]
aptPkgs = []

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@10.27.0 --activate",
  "pnpm install"
]

[phases.build]
cmds = [
  "pnpm --filter @marketing-autopilot/web build"
]

[start]
cmd = "pnpm --filter @marketing-autopilot/web start"
