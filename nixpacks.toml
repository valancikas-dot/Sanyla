[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "cd apps/web && pnpm build"
]

[start]
cmd = "cd apps/web && pnpm start"
